server:
  port: ${PORT:8080}

spring:
  application:
    name: moneyrocket

openai:
  api-key: ${OPENAI_API_KEY:}
  model: ${OPENAI_MODEL:gpt-5.2}

app:
  cors:
    allowed-origins:
      - https://moneyrocket-wdisshizqa-du.a.run.app
      - https://money-rocket.apps.tossmini.com
      - https://money-rocket.private-apps.tossmini.com
      - http://localhost:5173
      - http://127.0.0.1:5173
